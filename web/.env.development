# Loaded on every `pnpm dev` (mode=development). Matches `DEV_TENANT_ID` in
# `src/harnex_api/services/tenant/seed.py`.
#
# Leave `VITE_HARNEX_API_URL` unset here so the SPA calls same-origin `/v1/...`
# and Vite proxies to the API (see `vite.config.ts`).

VITE_HARNEX_DEV_TENANT=11111111-1111-1111-1111-111111111111
